import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const SigninScreen = () => {

    return (
        <View>
            <Text style={ { fontSize: 45 } }>SignInScreen</Text>
        </View>
    );
}

const styles = StyleSheet.create({

});

export default SigninScreen;
